# Database Configuration
# For Render/Production: Use your Supabase or Render PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:port/database
DATABASE_TYPE=postgresql

# For Local Development: Leave DATABASE_URL empty to use SQLite
# DATABASE_URL=
# DATABASE_TYPE=sqlite

# Application Secret Key (REQUIRED - Generate a random string!)
# You can generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-this-to-random-string

# Supabase Configuration (Optional - for client-side features)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here
